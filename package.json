{
  "name": "lol-stats",
  "version": "2.0.0",
  "description": "League of Legends statistics archive worker",
  "type": "module",
  "scripts": {
    "dev": "npm run dev:kv && wrangler dev",
    "dev:kv": "wrangler kv key put ConfigActive --path config/ConfigActive.json --binding=lol-stats-kv --local && wrangler kv key put ConfigArchive --path config/ConfigArchive.json --binding=lol-stats-kv --local",
    "deploy": "wrangler deploy",
    "build": "echo 'Build complete - ES modules ready'",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260425.1",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.1",
    "vitest": "^4.1.5",
    "wrangler": "^4.85.0"
  }
}
